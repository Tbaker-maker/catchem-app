{
  "name": "catchem-app",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "landers": "node scripts/generate-landers.mjs",
    "build": "node scripts/generate-landers.mjs && vite build",
    "preview": "vite preview",
    "deploy": "wrangler deploy && node ../Catchem-data/scripts/smoke-test.mjs",
    "deploy:site": "node scripts/build-public-site.mjs && wrangler deploy -c wrangler.site.jsonc && node ../Catchem-data/scripts/smoke-test.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.6.0",
    "vite": "^6.3.5"
  }
}
